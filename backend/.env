# ── Server ──────────────────────────────────────────
PORT=5000
NODE_ENV=development

# ── MongoDB Atlas ────────────────────────────────────
MONGO_URI=mongodb+srv://yashworkcode:20006yasH@mytunes.i6a8s5p.mongodb.net/?appName=mytunes

# ── JWT ──────────────────────────────────────────────
JWT_SECRET=fK9xQ2mV7pL4nR8tY3wZ6cH1jD5sA9uBqE2gN7kM
JWT_EXPIRES_IN=7d

# ── Cloudinary ───────────────────────────────────────
CLOUDINARY_CLOUD_NAME=dqvqgmwb8
CLOUDINARY_API_KEY=791658758323545
CLOUDINARY_API_SECRET=Y697RBwVZsppuPbeKl6HSHvER9I

# ── Encryption (AES-256, must be exactly 64 hex chars = 32 bytes) ──
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# ── Frontend origin (CORS) ───────────────────────────
# This MUST match the exact origin (protocol+host+port) your frontend is served from,
# or every API call from the browser will fail with a CORS error.
#   npx serve            -> usually http://localhost:3000
#   VSCode Live Server   -> usually http://127.0.0.1:5500
#   python -m http.server -> http://localhost:8000 (or whatever port you pass)
# Check the URL in your browser's address bar and put it here exactly.
CLIENT_URL=http://localhost:3000
